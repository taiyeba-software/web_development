//header scroll

let nav = document.querySelector(".navbar");
window.onscroll=function()
{
    if(document.documentElement.scrollTop>20) //The documentElement property returns a document's element (as an Element object).//
    {

    }
}